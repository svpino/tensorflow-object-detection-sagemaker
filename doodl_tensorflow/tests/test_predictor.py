from doodl import Processor, Configuration


def test_x():
    configuration = Configuration({
        "file": "test.jpg"
    })
    #processor = Processor.factory(configuration)
    #result = processor.inference()
    print("test")

